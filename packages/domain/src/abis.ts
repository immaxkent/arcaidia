/**
 * ABI barrel.
 *
 * Populated by the Foundry build in WP-01 so that no downstream package ever
 * declares a fragment of Arcaidia's interface for itself. Empty until the
 * contracts exist.
 */

export const ABIS = {} as const;
