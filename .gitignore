node_modules/
dist/
out/
cache/
coverage/
.env
.env.local
*.log
.DS_Store
broadcast/

# apps/web build output not already covered by its own .gitignore
apps/web/.output/
apps/web/.vinxi/

# graph-cli writes here when run from the repo root without --output-dir
/build/

# the live solver's own decision log (packages/agent/src/entrypoint/main.ts)
solver-decisions*.jsonl
.circle-recovery/

# Per-operator solver env files (WP-28.6 / WP-31) — keys, never committed
.env.solver-*
!.env.solver-*.example

subgraph/build-arc
